services:
  nacos:
    image: nacos/nacos-server:v2.4.3
    container_name: demo-nacos
    environment:
      - MODE=standalone
      - JVM_XMS=256m
      - JVM_XMX=256m
    ports:
      - "8848:8848"
      - "9848:9848"
    volumes:
      - nacos-data:/home/nacos/data
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8848/nacos/v1/console/health/readiness || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 40s
    networks:
      - edu-net
    mem_limit: 512m
    cpus: 0.5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: always

  postgres:
    image: postgres:16
    container_name: demo-postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      - POSTGRES_DB=edu_platform
      - LANG=C.UTF-8
      - LC_ALL=C.UTF-8
    ports:
      - "5432:5432"
    volumes:
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres-data:/var/lib/postgresql/data
    command: postgres -c shared_buffers=256MB -c effective_cache_size=768MB -c wal_buffers=16MB -c max_connections=100 -c random_page_cost=1.1
    mem_limit: 1g
    cpus: 1.0
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - edu-net
    restart: always

  redis:
    image: redis:7.0
    container_name: demo-redis
    ports:
      - "16379:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required} --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru --save 900 1 --save 300 10
    volumes:
      - redis-data:/data
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: [ "CMD", "redis-cli", "-a", "${REDIS_PASSWORD:?REDIS_PASSWORD is required}", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - edu-net
    restart: always

  gateway:
    image: demo-gateway:latest
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        MODULE_NAME: gateway
    container_name: demo-gateway
    ports:
      - "8090:8090"
    environment:
      - NACOS_ADDR=nacos:8848
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN is required}
      - JAVA_OPTS=-Xms128m -Xmx256m -XX:MaxRAMPercentage=75 -XX:+UseG1GC -XX:+UseStringDeduplication
      - SPRING_THREADS_VIRTUAL_ENABLED=false
      - SPRING_CLOUD_LOADBALANCER_CACHE_ENABLED=true
      - SPRING_CLOUD_GATEWAY_GLOBALCORS_CORS-CONFIGURATIONS_[/**]_ALLOWED-ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost
    mem_limit: 768m
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      nacos:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -s -o /dev/null --max-time 3 http://localhost:8090/ || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 60s
    networks:
      - edu-net
    restart: always

  user-service:
    image: demo-user-service:latest
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        MODULE_NAME: user-service
    container_name: demo-user-service
    environment:
      - NACOS_ADDR=nacos:8848
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/edu_platform?characterEncoding=UTF-8
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      - SPRING_REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN is required}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:?MINIO_ACCESS_KEY is required}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:?MINIO_SECRET_KEY is required}
      - JAVA_OPTS=-Xms128m -Xmx256m -XX:MaxRAMPercentage=75 -XX:+UseG1GC -XX:+UseStringDeduplication
      - SESSION_TIMEOUT_SECONDS=604800
      - SPRING_CLOUD_LOADBALANCER_CACHE_ENABLED=true
    mem_limit: 768m
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      nacos:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8081/actuator/health || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 60s
    networks:
      - edu-net
    restart: always

  course-service:
    image: demo-course-service:latest
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        MODULE_NAME: course-service
    container_name: demo-course-service
    environment:
      - NACOS_ADDR=nacos:8848
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/edu_platform?characterEncoding=UTF-8
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      - SPRING_REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN is required}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:?MINIO_ACCESS_KEY is required}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:?MINIO_SECRET_KEY is required}
      - JAVA_OPTS=-Xms128m -Xmx256m -XX:MaxRAMPercentage=75 -XX:+UseG1GC -XX:+UseStringDeduplication
      - SPRING_CLOUD_LOADBALANCER_CACHE_ENABLED=true
    mem_limit: 768m
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      nacos:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8082/actuator/health || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 60s
    networks:
      - edu-net
    restart: always

  homework-service:
    image: demo-homework-service:latest
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        MODULE_NAME: homework-service
    container_name: demo-homework-service
    environment:
      - NACOS_ADDR=nacos:8848
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/edu_platform?characterEncoding=UTF-8
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      - SPRING_REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN is required}
      - JAVA_OPTS=-Xms128m -Xmx256m -XX:MaxRAMPercentage=75 -XX:+UseG1GC -XX:+UseStringDeduplication
      - SPRING_CLOUD_LOADBALANCER_CACHE_ENABLED=true
    mem_limit: 768m
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      nacos:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8083/actuator/health || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 60s
    networks:
      - edu-net
    restart: always

  progress-service:
    image: demo-progress-service:latest
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        MODULE_NAME: progress-service
    container_name: demo-progress-service
    ports:
      - "8084:8084"
    environment:
      - NACOS_ADDR=nacos:8848
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/edu_platform?characterEncoding=UTF-8
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      - SPRING_REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD is required}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN is required}
      - JAVA_OPTS=-Xms128m -Xmx256m -XX:MaxRAMPercentage=75 -XX:+UseG1GC -XX:+UseStringDeduplication
      - SPRING_CLOUD_LOADBALANCER_CACHE_ENABLED=true
    mem_limit: 768m
    cpus: 1.0
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      nacos:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8084/actuator/health || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 60s
    networks:
      - edu-net
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: demo-frontend
    ports:
      - "5173:80"
    mem_limit: 256m
    cpus: 0.25
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:80 || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    depends_on:
      gateway:
        condition: service_healthy
    networks:
      - edu-net
    restart: always

  minio:
    image: minio/minio:RELEASE.2024-01-18T22-51-28Z
    container_name: demo-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER:?MINIO_ROOT_USER is required}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD is required}
    volumes:
      - minio-data:/data
    command: server /data/disk{1...4} --console-address ":9001"
    mem_limit: 512m
    cpus: 0.5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: [ "CMD", "mc", "ready", "local" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - edu-net
    restart: always

  hindsight:
    image: ghcr.io/vectorize-io/hindsight:latest
    container_name: demo-hindsight
    ports:
      - "8888:8888"
      - "9999:9999"
    environment:
      - HINDSIGHT_API_LLM_PROVIDER=ollama
      - HINDSIGHT_API_LLM_API_KEY=ollama
      - HINDSIGHT_API_LLM_MODEL=qwen2.5:3b
      - HINDSIGHT_API_LLM_BASE_URL=http://ollama-json-proxy:11435/v1
      - OLLAMA_KEEP_ALIVE=-1
      - HINDSIGHT_API_LLM_MAX_CONCURRENT=4
      - HINDSIGHT_API_LOG_LEVEL=info
      - HF_ENDPOINT=https://huggingface.co
      - HINDSIGHT_API_EMBEDDINGS_PROVIDER=local
      - HINDSIGHT_API_EMBEDDINGS_LOCAL_MODEL=BAAI/bge-m3
      - HINDSIGHT_API_RERANKER_PROVIDER=rrf
      - HINDSIGHT_API_WORKER_ID=hindsight-worker-1
    volumes:
      - hindsight-data:/home/hindsight/.pg0
      - hindsight-cache:/home/hindsight/.cache/huggingface
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8888/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 300s
    mem_limit: 4g
    memswap_limit: 4g
    cpus: 4.0
    stop_grace_period: 60s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - edu-net
    restart: always

  ollama-json-proxy:
    image: python:3.12-slim
    container_name: demo-ollama-json-proxy
    command: python /app/json-ollama-proxy.py
    volumes:
      - ./ops/scripts/hindsight/json-ollama-proxy.py:/app/json-ollama-proxy.py
    environment:
      - OLLAMA_HOST=host.docker.internal
      - OLLAMA_PORT=11434
      - PROXY_PORT=11435
      - LLM_TIMEOUT=900
    ports:
      - "11436:11435"
    networks:
      - edu-net
    restart: unless-stopped

networks:
  edu-net:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  nacos-data:
  minio-data:
  hindsight-data:
  hindsight-cache:
